version: '3'
services:
  database:
    image: 'postgres:latest'

    ports:
      - ${DB_PORT}:5432

    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASS}
      POSTGRES_DB: ${DB_NAME}